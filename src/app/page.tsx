export default function Home() {
  // console.log(process.env.NODE_ENV, process.env.ENV_ARG, process.env.OPEN_API_KEY, "hihi")
  return (
    <main className='flex min-h-screen flex-col items-center justify-between p-24'>
      <p>INIT</p>
    </main>
  )
}
